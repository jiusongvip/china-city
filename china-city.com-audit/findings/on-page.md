# On-Page SEO Findings

- ✅ Title：`China City Guide 2026: Compare & Plan the Best Cities to Visit`（57 字符，含核心词）。
- ✅ Meta description 语义完整，含转化导向（compare / budget / season / rail）。
- ✅ H1 唯一（`Find the right Chinese city for your trip.`），全站仅 1 个。
- ✅ H2（14 个）/ H3（8 个）层级合理，每个 section 一个 H2。
- ✅ Open Graph 完整：`og:type/title/description/url/image` + `og:image:width/height/alt`。
- ✅ `twitter:card = summary_large_image` + `twitter:image`。
- ✅ `canonical` 已设置，`robots = index,follow`。
- ⚠️ canonical 尾斜杠与 `trailingSlash: 'always'` 不一致（详见 technical 分类）。
